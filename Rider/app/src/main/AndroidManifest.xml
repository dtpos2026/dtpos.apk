<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!--
      ===== v1.28.7 — the permissions the app actually exercises =====

      ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION
        The rider screen calls navigator.geolocation.watchPosition to report
        the delivery position, and staffLocation tracks it while on duty.
        Capacitor's BridgeWebChromeClient requests the Android permission at
        runtime when the WebView asks — but a permission the manifest never
        declared cannot be requested, so the request would be refused outright
        and live tracking would silently never start.

        ACCESS_BACKGROUND_LOCATION is deliberately NOT declared. Tracking runs
        while the rider has the app open; asking for the background grant
        triggers a far heavier consent flow and a Play policy review for
        something this app does not do.

      VIBRATE
        ReadyNotificationBus vibrates when an order is ready. A normal
        permission — no prompt — but absent it does nothing, silently.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
</manifest>
