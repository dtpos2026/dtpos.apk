<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!--
          Messages sent by push-dispatch carry channel_id "dt_orders"; this is
          the fallback for anything that arrives without one, so a notification
          is never dropped into a channel that does not exist.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="dt_orders" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!--
      ===== v1.28.7 — the permissions the app actually exercises =====

      INTERNET / ACCESS_NETWORK_STATE
        Everything the app does is a request to the restaurant's server.

      ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION
        REPORTED: "location permission maangta hai, deny karta hai access,
        allow hi nahi kar sakte." The customer screen calls
        navigator.geolocation.getCurrentPosition to fill in a delivery address
        (OnlineOrderPage). Capacitor's BridgeWebChromeClient answers the
        WebView's geolocation prompt by requesting the Android permission at
        runtime — but a permission the manifest never declared CANNOT be
        requested, so the prompt was refused outright and there was no way for
        the customer to allow it. Declaring them is what makes the dialog
        appear at all.

      POST_NOTIFICATIONS
        Android 13+ requires an explicit grant before an app may post a
        notification. Without it the request PushNotifications.requestPermissions()
        makes is refused, and the customer never sees "your order is on the way".

      VIBRATE
        ReadyNotificationBus calls navigator.vibrate when an order changes
        state. A normal permission — granted at install, no prompt — but
        absent it simply does nothing, silently.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
      Location is used to fill in an address, not to find the restaurant. A
      device with no GPS hardware must still be able to install and order, so
      this is declared optional rather than required — Play would otherwise
      hide the app from those devices entirely.
    -->
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
</manifest>
