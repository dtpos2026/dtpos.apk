<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!--
          Messages sent by push-dispatch carry channel_id "dt_orders"; this is
          the fallback for anything that arrives without one, so a notification
          is never dropped into a channel that does not exist.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="dt_orders" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!--
      ===== v1.28.7 — the permissions the app actually exercises =====

      ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION
        OrderTakerPortalPage imports hasLocationConsent / startLocationTracking
        from staffLocation, so this app reports its position exactly as the
        rider app does — and was missing the permissions that make the runtime
        request possible at all.

      VIBRATE
        ReadyNotificationBus vibrates when an order is ready.

      POST_NOTIFICATIONS  (v1.29.9)
        Android 13+ refuses to post a notification until the user has granted
        this, and a permission the manifest never declared cannot even be
        REQUESTED — so PushNotifications.requestPermissions() was refused
        outright and there was no way for the order taker to say yes. The
        Customer app has carried this since v1.28.7; this app was left behind,
        which is why no alert ever arrived when a new order came in.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
</manifest>
